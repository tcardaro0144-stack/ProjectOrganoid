// Copyright Epic Games, Inc. All Rights Reserved.

#include "ProjectOrganoidDamageable.h"

// BlueprintNativeEvent defaults are provided by UHT (inline empty _Implementation
// stubs in ProjectOrganoidDamageable.generated.h). Do not redefine them here —
// that causes C2084 ("function already has a body"). Hosts override via
// ResolveWeakPoint_Implementation / ApplyOrganoidHit_Implementation on the actor.
